// Floor on how short a tenancy agreement's term can be — Reallow-wide policy, not
// something an individual landlord can undercut. Each listing can set its own
// (higher) minimum on top of this — see src/lib/fees.ts and Property.minimumTermMonths.
export const MINIMUM_LEASE_TERM_MONTHS = 6;
